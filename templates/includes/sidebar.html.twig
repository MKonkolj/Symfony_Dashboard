<div class="sidebar">
    <img src="{{ asset("images/universal-logo-small.png") }}" alt="universal logo">
    <ul class="sidebar-menu">
        {% if is_granted("ROLE_ADMIN") %}
            <li><a href="{{ path('dashboard_developers') }}">Developers</a></li>
            <li><a href="{{ path('dashboard_clients') }}">Clients</a></li>
        {% endif %}
            <li><a href="{{ path('dashboard_my-profile') }}">My Profile</a></li>
            <li><a href="{{ path('dashboard_logout') }}">Logout</a></li>
    </ul>
</div>